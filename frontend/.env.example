# The dev server proxies /api to the backend (see vite.config.ts).
# No build-time env needed for local dev. Set this only if you deploy the
# frontend separately and call the API on another origin.
# VITE_API_BASE=https://api.example.com
